schema_version: 1

context:
  python_min: '3.10'
  name: dagster-cloud
  version: "1.12.22"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/${{ name | replace('-', '_') }}-${{ version }}.tar.gz
  sha256: 2328caed51d122e12e0d8303d02944ca9fec8905a1ca3b011256ed39592309a6

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - hatchling >=1.19.0
  run:
    - python >=${{ python_min }}
    - dagster ==${{ version }}
    - dagster-cloud-cli ==${{ version }}
    - dagster-shared ==${{ version }}
    - pex >=2.1.132,<2.60.0
    - questionary
    - requests
    - typer >=0.15.0

tests:
  - python:
      imports:
        - dagster_cloud
      python_version: ${{ python_min }}.*
  - requirements:
      run:
        - pip
        - python ${{ python_min }}.*
    script:
      - pip check

about:
  license: Apache-2.0
  license_file: LICENSE
  summary: Cloud support for Dagster
  homepage: https://github.com/dagster-io/dagster-cloud

extra:
  recipe-maintainers:
    - jaiswal-ankur
    - geoHeil
    - tmirko
    - milicevica23
